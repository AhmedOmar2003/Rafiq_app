# Supabase CLI config — Rafiq
# https://supabase.com/docs/guides/cli/config
#
# This file is checked into VCS. Secrets stay in `supabase/.env.local` (ignored).

project_id = "rafiq"

[api]
enabled = true
port = 54321
schemas = ["public", "storage", "graphql_public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54322
shadow_port = 54320
major_version = 15

[studio]
enabled = true
port = 54323

[storage]
enabled = true
file_size_limit = "50MiB"

[auth]
enabled = true
site_url      = "http://localhost:3000"
additional_redirect_urls = [
  "rafiqapp://reset-password",
  "rafiqapp://oauth-callback",
  "http://localhost:3000/auth/callback",
]
jwt_expiry            = 3600        # 1 hour access token
enable_refresh_token_rotation = true
refresh_token_reuse_interval  = 10

[auth.email]
enable_signup         = true
double_confirm_changes = true
enable_confirmations  = true                # OTP required to sign up
secure_password_change = true

[auth.email.template.confirmation]
subject = "كود تأكيد رفيق"
content_path = "supabase/templates/confirmation.html"

[auth.email.template.recovery]
subject = "كود تغيير كلمة السر — رفيق"
content_path = "supabase/templates/recovery.html"

[auth.external.google]
enabled    = true
client_id  = "env(GOOGLE_CLIENT_ID)"
secret     = "env(GOOGLE_CLIENT_SECRET)"
redirect_uri = "https://<project-ref>.supabase.co/auth/v1/callback"

[edge_runtime]
enabled = true
policy  = "oneshot"
inspector_port = 8083
