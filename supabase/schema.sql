-- =============================================================================
-- Rafiq — schema bootstrap (DEPRECATED, kept for compatibility)
-- -----------------------------------------------------------------------------
-- The authoritative schema lives in supabase/migrations/ — apply with
--   supabase db push     (remote)
--   supabase db reset    (local)
--
-- This file is intentionally empty; older scripts that called
-- `psql -f supabase/schema.sql` should be updated to use the migrations dir.
-- =============================================================================

-- no-op
select 1;
